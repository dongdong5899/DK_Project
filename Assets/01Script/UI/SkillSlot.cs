using UnityEngine;

namespace DKProject.UI
{
    public class SkillSlot : Slot
    {
        //뭐 SO면 SO 넣고 Key값으로 가져오는거면 Key값으로 가져가야지
        public void SetSkill()
        {
            
        }

        public void RemoveSkill()
        {
            RemoveIcon();
        }
    }
}
